# python virtual environment (created by setup_hybrid)
.venv/

# cloned base tool (fetched by setup_hybrid)
claude-code-quality-hook/

# AF runtime history
af_violation_history.json
.claude/af_violation_history.json

# base config generated by setup (keep the template, ignore the live copy if you prefer)
# .quality-hook.json
